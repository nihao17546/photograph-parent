<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nihaov.photograph.dao.IIMageDAO">
    <select id="selectRandom" resultType="com.nihaov.photograph.pojo.po.ImagePO">
        select * from tb_image order by rand() limit #{limit}
    </select>
</mapper>