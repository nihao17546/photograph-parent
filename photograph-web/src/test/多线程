1.线程生命周期
  创建-》就绪-》运行-》阻塞-》死亡
2.怎样使线程阻塞
  a.sleep()
  b.获取锁,锁被其他线程占用
  c.suspend()
  d.等待通知notify
  e.调用阻塞io方法,在方法返回前
3.什么时候会释放锁
  a.执行结束
  b.break,return终止
  c.抛异常
  d.执行了锁的wait()方法
  (sleep,yield,suspend不会)
4.Lock  Condition